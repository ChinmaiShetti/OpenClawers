"""src/vector_db/search.py — Qdrant semantic search (reads only)."""
# TODO: implement semantic search for chat context assembly
async def search(query_vector: list[float], top_k: int = 5) -> list[dict]:
    return []
